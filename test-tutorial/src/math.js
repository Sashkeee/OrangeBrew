/**
 * Эта функция просто складывает два числа.
 * Мы будем использовать её для изучения юнит-тестов.
 */
export const add = (a, b) => {
    return a + b;
};

/**
 * Эта функция проверяет, является ли число четным.
 */
export const isEven = (num) => {
    return num % 2 === 0;
};
